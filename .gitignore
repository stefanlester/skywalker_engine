# Environment / secrets
.env

# Local data volumes (DB, MinIO, SFTP keys, etc.)
db-data/

# Temporary upload / scratch directory
tmp/

# Build artifacts
*.exe
skywalker
skywalker.exe

# Build in module mode — do not commit the vendor tree.
/vendor/
